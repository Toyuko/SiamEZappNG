# Copy to keystore.properties and fill in after creating your keystore.
# Do not commit keystore.properties or *.keystore files.

storePassword=your-store-password
keyPassword=your-key-password
keyAlias=siamez-release
storeFile=siamez-release.keystore
